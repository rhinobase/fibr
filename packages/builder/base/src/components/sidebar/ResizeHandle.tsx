import {
  PanelResizeHandle,
  type PanelResizeHandleProps,
} from "react-resizable-panels";

export function ResizeHandle(props: PanelResizeHandleProps) {
  return <PanelResizeHandle {...props} />;
}
